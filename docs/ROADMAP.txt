================================================================================
PURSUIT OF PEACE — DEVELOPMENT ROADMAP
================================================================================

Versioning follows Semantic Versioning (MAJOR.MINOR.PATCH).
  MAJOR — breaking save-file or architecture change.
  MINOR — new feature tier complete.
  PATCH — bug fixes, balance tweaks, content additions.

================================================================================
v1.0.0 — ACTIVE ACTION DISPATCH + TIER 4 COMPLETE  [✓ RELEASED]
================================================================================

STATUS: ✓ COMPLETE

Architecture
  [✓] Active Action Dispatch (AAD) — all actions described in data/actions/*.json.
  [✓] ResolverRegistry — auto-discovers resolvers; no manual registration.
  [✓] ActionDispatcher — validates, loads, and chains resolver calls.
  [✓] 40 action JSON files; 30 resolver modules; 0 unavailable at startup.
  [✓] Engine fully wired to AAD — zero hardcoded if/elif action chains.
  [✓] Data log event emitted for every dispatched action.

Tier 4 — Shop Economy
  [✓] shop_inventory arrays in services.json (marketplace, alchemy hall, smithy).
  [✓] shop.buy resolver — validates stock, deducts gold, adds item.
  [✓] shop.sell resolver — validates ownership, unequipped, awards 40% value.
  [✓] Shop UI panel — auto-shows when location has shop_inventory.

Tier 4 — Data-Driven Loot
  [✓] All 12 enemies have loot_table, loot_chance, loot_count, gold_min, gold_max.
  [✓] combat.award_loot resolver — rolls chance, dispatches add_item + add_gold.
  [✓] award_loot chained as sub-action on enemy death (combat.attack resolver).

Step 8 — Remaining System Gaps
  [✓] tick_buffs dispatched after every combat round.
  [✓] advance_year dispatched via _tick_year() every 30 actions.
  [✓] Exile path wired through Engine._on_exile() with profile deletion.
  [✓] Fishing stubs — cast_line + fishing_tick resolvers; fish_pool.json created.

Developer Tools
  [✓] Validate Actions tab in DevTools.pyw.
  [✓] Generate Resolver tab in DevTools.pyw.
  [✓] --debug flag for main.pyw.
  [✓] Startup validation in ActionDispatcher.load_actions().

Documentation
  [✓] ARCHITECTURE.md — full AAD description, entity ID scheme, message flow.
  [✓] CONTRIBUTING.md — how to add actions, resolvers, shops, enemies, items.
  [✓] DATA_SCHEMA.md — action JSON schema, shop_inventory, loot fields, buff schema.
  [✓] CHANGELOG.md — v1.0.0 entry.
  [✓] KNOWN_GAPS.md — Tier 4 marked complete; future tiers assigned to v1.1.0+.
  [✓] KNOWN_ISSUES.md — stubs, shop, buff, year rollover issues removed.
  [✓] ROADMAP.txt — semantic versioning, tiers 1–4 complete.
  [✓] README.md — updated to v1.0.0 with full feature list.

================================================================================
v1.1.0 — FISHING MINI-LOOP + DYNAMIC SHOPS  [PLANNED]
================================================================================

  [ ] Fishing mini-loop — real-time scheduler, weighted fish_pool.json draw.
  [ ] UI feedback during fishing wait (countdown / animation).
  [ ] Dynamic shop restocking — shops restock on year rollover from templates.
  [ ] Shops can sell out of limited-stock items.
  [ ] Coliseum placeholder — at minimum a wave-fight mode or description text.
  [ ] ISSUE-003 fix — reset unknown current_location_id to city_entrance on load.

================================================================================
v1.2.0 — CRAFTING + LEVELLING  [PLANNED]
================================================================================

  [ ] data/recipes.json schema.
  [ ] craft_item action + crafting.craft resolver.
  [ ] Alchemy Hall UI: recipe browser, ingredient check, craft button.
  [ ] XP system — gain XP from kills; level-up thresholds in config.json.
  [ ] Level-based stat bonuses or location unlock gates.
  [ ] Item lore / flavour text shown in inventory detail panel.

================================================================================
v1.3.0 — NPC MERCHANTS + EVENTS  [PLANNED]
================================================================================

  [ ] NPC definitions data format (name, dialogue, shop, faction).
  [ ] NPC encounters in dungeon rooms and city locations.
  [ ] Dialogue tree data format + resolver.
  [ ] data/events/events.json — random events on location entry or year rollover.
  [ ] Event resolver reads events.json, applies random outcomes.
  [ ] Random city events: market crash (prices up), bumper harvest (tax reduced).

================================================================================
v2.0.0 — MULTIPLAYER PROFILES + LEADERBOARD  [FUTURE / SPECULATIVE]
================================================================================

  [ ] Multiple profiles on one machine — profile selector on launch.
  [ ] Optional shared leaderboard (longest run, most gold, most kills).
  [ ] Profile export/import for sharing.
  [ ] Potential online highscore submission (opt-in).

================================================================================
